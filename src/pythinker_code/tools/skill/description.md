Read the instructions for an available skill by name.

Use this before applying a workflow skill so you follow its exact steps. If the skill has a `<name>-local` companion, the returned content includes the local specialization after the core skill.
